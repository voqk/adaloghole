"""Admin portal — the user's control panel.  [STUB]

    GET  /admin           render the current settings form
    POST /admin           save edited settings (key, model, prompt, program, thresholds)
    POST /admin/test      upload one frame and show the live classification (no muting)

Lets a user enter their own Anthropic key and tune the prompt without touching code.
"""

# TODO: from fastapi import APIRouter, Request, Form, UploadFile
# TODO: from fastapi.templating import Jinja2Templates
# TODO: from .settings import load_settings, save_settings
# TODO: router = APIRouter(prefix="/admin")
# TODO: templates = Jinja2Templates(directory="templates")
