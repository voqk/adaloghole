# Use recent versions. anthropic must support output_config structured outputs.
fastapi
uvicorn[standard]
anthropic
jinja2
python-multipart      # form posts / file uploads in the admin portal
opencv-python-headless  # webcam Sensor (no GUI deps)
httpx                 # Sensor -> Brain localhost HTTP seam; also the test client

# Dev / test
pytest
