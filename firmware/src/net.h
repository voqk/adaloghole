#pragma once
// Wi-Fi + talking to the LAN server.  [STUB]

// TODO: bool net_connect_wifi();
// TODO: a Result { enum Action { NONE, MUTE, UNMUTE } action; bool muted; }
// TODO: Result net_classify(jpeg buffer);   // HTTP POST SERVER_URL, parse JSON reply
