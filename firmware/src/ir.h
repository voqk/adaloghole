#pragma once
// IR: learn the TV/soundbar codes (receiver) and send them (emitter).  [STUB]

// TODO: bool ir_init();
// TODO: ir_learn();        // capture a code from the real remote (IR receiver)
// TODO: ir_send_mute();    // replay the stored mute code (STEMMA IR emitter)
// TODO: ir_send_unmute();
