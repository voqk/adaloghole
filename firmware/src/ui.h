#pragma once
// Faces-you side: status LCD + rotary knob override.  [STUB] (Stage 2)

// TODO: bool ui_init();
// TODO: ui_update(result);   // show current label / muted state on the LCD
// TODO: ui_poll_knob();      // rotary encoder: manual override / lock a mode
