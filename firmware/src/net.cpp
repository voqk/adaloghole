// Wi-Fi + LAN server implementation.  [STUB]
#include "net.h"

// TODO: WiFi.begin(WIFI_SSID, WIFI_PASSWORD); wait for connect.
// TODO: HTTPClient POST to SERVER_URL with Content-Type: image/jpeg, body = JPEG.
// TODO: ArduinoJson parse of {label, confidence, reason, action, muted}.
// Plain HTTP — no TLS, no API key on the device.
