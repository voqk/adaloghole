#pragma once
// OV2640 camera: init and grab a single JPEG frame.  [STUB]

// TODO: bool camera_init();                    // configure pins, PSRAM framebuffer, JPEG
// TODO: capture returns a JPEG buffer + length (e.g. via esp_camera_fb_get()).
